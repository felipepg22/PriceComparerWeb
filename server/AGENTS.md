# Server rule router

The root `AGENTS.md` and its required rules apply here.

For any change under `server/`, read `../rules/backend.md`. For production-code changes also read `../rules/architecture.md`; before completing code, configuration, or test changes read `../rules/verification.md`. Load `../rules/domain.md` and `../rules/security.md` when their triggers in `../rules/README.md` match.
